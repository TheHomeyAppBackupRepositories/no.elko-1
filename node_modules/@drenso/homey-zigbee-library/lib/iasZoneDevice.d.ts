import { ZigBeeDevice } from 'homey-zigbeedriver';
import { ZCLNode, ZoneStatus, ZoneStatusChangedPayload } from 'zigbee-clusters';
export default function initIasZoneDevice(device: ZigBeeDevice, zclNode: ZCLNode, capabilityIds: string[], statusParsers: (((payload: ZoneStatusChangedPayload) => any) | ZoneStatus)[], endpointId?: number): Promise<void>;
