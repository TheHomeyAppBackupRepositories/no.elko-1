import { ZigBeeDevice } from 'homey-zigbeedriver';
import { ZCLNode, ZoneStatus, ZoneStatusChangedPayload } from 'zigbee-clusters';
export default function initIasZoneDevice(device: ZigBeeDevice, zclNode: ZCLNode, capabilityIds: string[], statusParsers: (((payload: ZoneStatusChangedPayload) => any) | ZoneStatus)[], // eslint-disable-line @typescript-eslint/no-explicit-any
endpointId?: number, autoEnrollResponse?: boolean): Promise<void>;
