import { ReadOnlyArgumentOverrides } from '../lib/attributeDevice';
import { ZigBeeDevice } from 'homey-zigbeedriver';
import { ZCLNode } from 'zigbee-clusters';
type ArgumentOverrides = ReadOnlyArgumentOverrides;
export default function initTargetTemperatureDevice(device: ZigBeeDevice, zclNode: ZCLNode, { capabilityId, cluster, attributeName, minChange, maxInterval, endpointId, }?: Partial<ArgumentOverrides>): Promise<void>;
export {};
