import { ZCLNode } from 'zigbee-clusters';
import { ZigbeeFactorDevice } from '../lib/helper/deviceFactor';
type ArgumentOverrides = {
    endpointId?: number;
    noPowerFactorReporting?: boolean;
};
export default function initMeteringDevice(device: ZigbeeFactorDevice, zclNode: ZCLNode, { endpointId, noPowerFactorReporting, }?: Partial<ArgumentOverrides>): Promise<void>;
export {};
