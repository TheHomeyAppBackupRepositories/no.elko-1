import { ZigBeeDevice } from 'homey-zigbeedriver';
import { ZCLNode } from 'zigbee-clusters';
type ArgumentOverrides = {
    capabilityId: string;
    endpointId?: number;
    maxInterval?: number;
    pollInterval?: number;
};
export default function initOnOffDevice(device: ZigBeeDevice, zclNode: ZCLNode, { capabilityId, endpointId, maxInterval, pollInterval, }?: Partial<ArgumentOverrides>): Promise<void>;
export {};
