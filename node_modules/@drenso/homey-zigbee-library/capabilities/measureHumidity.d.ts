import { ZigBeeDevice } from 'homey-zigbeedriver';
import { ZCLNode } from 'zigbee-clusters';
import { ReadOnlyArgumentOverrides } from '../lib/attributeDevice';
type ArgumentOverrides = ReadOnlyArgumentOverrides;
export default function initMeasureHumidityDevice(device: ZigBeeDevice, zclNode: ZCLNode, { capabilityId, cluster, attributeName, minChange, maxInterval, endpointId, }?: Partial<ArgumentOverrides>): Promise<void>;
export {};
