import { ZCLNode } from 'zigbee-clusters';
import { ZigbeeFactorDevice } from '../lib/helper/deviceFactor';
type ArgumentOverrides = {
    endpointId?: number;
};
export default function initMeteringDevice(device: ZigbeeFactorDevice, zclNode: ZCLNode, { endpointId, }?: Partial<ArgumentOverrides>): Promise<void>;
export {};
