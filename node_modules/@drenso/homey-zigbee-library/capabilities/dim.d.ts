import { ZigBeeDevice } from 'homey-zigbeedriver';
import { ZCLNode } from 'zigbee-clusters';
type ArgumentOverrides = {
    capabilityId: string;
    onOffCapabilityId: string;
    maxDimValue: number;
    minChange?: number;
    endpointId?: number;
};
export default function initDimDevice(device: ZigBeeDevice, zclNode: ZCLNode, { capabilityId, onOffCapabilityId, maxDimValue, minChange, endpointId, }?: Partial<ArgumentOverrides>): Promise<void>;
export {};
