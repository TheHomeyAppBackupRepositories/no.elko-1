import { ZigBeeDevice } from 'homey-zigbeedriver';
import { ZCLNode } from 'zigbee-clusters';
type ArgumentOverrides = {
    capabilityId: string;
    onOffCapabilityId: string | false;
    maxDimValue: number;
    maxInterval?: number;
    minChange?: number;
    endpointId?: number;
};
export default function initDimDevice(device: ZigBeeDevice, zclNode: ZCLNode, { capabilityId, onOffCapabilityId, maxDimValue, maxInterval, minChange, endpointId, }?: Partial<ArgumentOverrides>): Promise<void>;
export {};
