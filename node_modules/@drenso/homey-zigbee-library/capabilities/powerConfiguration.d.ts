import { ZigBeeDevice } from 'homey-zigbeedriver';
import { ZCLNode } from 'zigbee-clusters';
type ArgumentOverrides = {
    capabilityId: string;
    endpointId?: number;
};
export default function initPowerConfigurationDevice(device: ZigBeeDevice, zclNode: ZCLNode, { capabilityId, endpointId, }?: Partial<ArgumentOverrides>): Promise<void>;
export {};
